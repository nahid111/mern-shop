import React, { Fragment } from "react";
import Products from './products/Products.jsx';

const Home = (props) => {
  return (
    <Fragment>
      <Products/>
    </Fragment>
  );
};

export default Home;
